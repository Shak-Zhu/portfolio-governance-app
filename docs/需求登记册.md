# 需求登记册｜v2.1

| ID | 优先级 | 需求 | 可验证验收条件 | 状态 |
|---|---|---|---|---|
| REQ-007 | P0 | 组合主数据 | 可创建、编辑、删除项目组合；每个组合有独立数据 | approved |
| REQ-008 | P0 | 项目与层级 | 可维护项目、父项目、Owner、Stage、Health、业务预期、Gate 和完成状态 | approved |
| REQ-009 | P0 | 步骤与甘特 | 每个步骤有名称、日期、状态；改日期后甘特立即按日/周/月重新落位 | approved |
| REQ-010 | P0 | 自定义 Stage 与归档 | 可新建 Stage；仅全部后代完成的顶级项目可整体归档 | approved |
| REQ-011 | P0 | 持久化与审计 | D1 保存主数据；重部署后数据仍在；每次写入留审计记录 | approved |
| REQ-012 | P1 | R2 备份 | 每日逻辑备份、保留策略和恢复演练 | blocked: R2 未开通 |
| REQ-013 | P0 | 原生 MCP | 经认证的读取/维护组合、项目、步骤、Stage、完成、归档工具 | approved |
| REQ-014 | P1 | Agent 使用说明 | 中文 MCP 接入、权限与工具示例 | approved |
| REQ-015 | P0 | 发布验收 | PM/QC 与 Human Owner 对范围、数据恢复、MCP、主数据和甘特验证 | planned |
| REQ-016 | P0 | 项目关联资料 | 每项目可维护至少 3 条"标题 + http(s) URL"资料；主数据展示数量/标题并可新窗口打开；增删改与审计持久化 | approved |
| REQ-017 | P0 | TBD 未排期工作包 | 无开始/结束日期的步骤在甘特图下方按项目分组展示固定尺寸灰色虚线卡；卡片显示项目归属；补齐日期并切为非 tbd 后自动进入日期轴 | approved |
| REQ-018 | P0 | 时间轴可靠性 | 日/周/月不静默截断；步骤按实际时间格边界落位；至少覆盖 366 天 / 260 周 / 120 月且无空白尾部或错误宽度 | approved |
| REQ-019 | P0 | 系统正式名称 | 所有用户可见网页、README 与产品文档统一使用"Shak 项目组合治理系统"；不改技术资源标识 | approved |
| REQ-020 | P0 | 原生认证 MCP 全量治理能力 | 通过正式 `/mcp` Streamable HTTP + 单用户 Bearer Token 暴露组合、项目、步骤、Stage、关联资料、甘特、审计、完成与整体归档全部治理动作；所有写入复用业务校验并以固定 actor `mcp:shak-pmo-owner` 审计；无匿名写入、无 OAuth / scope / KV / 通用 SQL 工具 | approved |
| REQ-021 | P0 | Agent 接入中心与一键安装指令 | 网页有 Codex / Cursor / 通用 Agent 接入区；每类提供一键复制的完整安装指令，安全合并已有 MCP 配置、不覆盖现有配置；登录会话内动态文案含真实 Bearer Token；Token 不出现于静态资产、Git、Skill、manifest 或报告 | approved |
| REQ-022 | P0 | 版本化 Agent Skill | Skill 权威源进入 Git，并作为网站公开只读资产发布；Codex 使用 `SKILL.md`，Cursor 使用 `.mdc` Rule；内容覆盖 MCP 全工具、业务约束、操作顺序、异常处理与验证；manifest 提供版本与 SHA-256 校验 | approved |
| REQ-023 | P0 | 单用户网页登录 | 原生邮箱+密码登录；Session Cookie `HttpOnly; Secure; SameSite=Strict; Path=/`，8 小时有效；未登录网页/API 返回 401/重定向；登出立即失效；凭据从 Worker Secret 读取，不进 D1/R2/Git/Skill/manifest/日志/报告 | approved |
| REQ-024 | P0 | MCP 强制 Bearer 鉴权 | `/mcp` 只接受 `Authorization: Bearer <token>` 校验 Worker Secret `SHAK_PMO_MCP_TOKEN`；缺失/错误为 JSON 401；不得返回 302、登录 HTML 或 OAuth 元数据 | approved |